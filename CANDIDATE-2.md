# No Left Click Rotation 候选版 2

当前实现以 `source/NoLeftClickRotationPython.py` V1 为唯一行为基准：永久 5ms 调度、空闲悬停 mat 缓存、20ms LightBox 分类、空白拖入模型起笔、相机锁定和右键 2ms 恢复均按 Python 状态机同步。

状态：候选测试版（RC2）

候选版 2 从候选版 1 派生，只处理左键。候选版 1 的快照保持不变。

## 与候选版 1 的差异

- 完全去掉 `Draw:Lock Camera` 的读取和写入。
- 完全去掉右键按下、右键抬起、右键回放及延迟重新锁定。
- 右键、Ctrl + 右键、Alt + 右键及其他右键组合全部由 ZBrush 原生处理。
- 插件启用、关闭或离开 Edit 模式时都不会改变相机锁定状态。
- 物理左键按下先不交给 ZBrush；首次 F12 只判断 Edit 和 View ID，区分 UI 与画布。

## 保留规则

- 插件默认启用，仅在 Edit 模式处理左键。
- UI 区域：在首次 F12 确认后补发原按下位置的左键按下，之后完整放行。
- 画布区域：先伪装为中键按下，再调用第二次 F12；第二次得到 `mat != 0` 才判定为模型并补发原位置的左键按下。
- 首次结果既不是 UI 也不是模型时，补发一次“左键按下＋抬起”，并在短暂分类窗口内吞掉移动，用系统箭头行为判断 LightBox。
- 判断为 LightBox 后进入独立的点击、双击和拖动处理。
- 判断为普通空白画布后，只保留最初的左键按下未交给 ZBrush；后续鼠标移动正常放行。
- 插件安装成功后立即启动永久的 5ms F12 循环，与 Python V1 的常驻轮询生命周期一致；不因左键、UI、画布、模型、Edit 模式或启用开关而停止。
- 手势阶段只决定本轮结果用于后台刷新、UI 路由、中键 `mat` 取样还是空白画布模型查询，不决定轮询是否运行。
- F12 循环使用单请求串行机制：上一轮 ZScript 返回后才发送下一轮，避免回调错位和堆积。
- 等待期间检测到 `mat != 0` 后停止查询，并立即在当前位置补发左键按下开始雕刻。
- 只有 ZBrush 窗口销毁或插件卸载时才关闭永久 F12 定时器。
- 保留 LightBox 点击、双击和拖动分类。
- 起笔延迟范围 0–10ms，默认 0ms。
- F12 内部桥继续在每次插件启动时写入当前 ZBrush 用户快捷键配置，且不会重复追加。
- F12 使用系统 `SendInput` 触发；只有当前前台进程属于 ZBrush 时才发送，避免投递到其他程序。
- 触发时临时释放实际按住的 Ctrl、Alt、Shift，发送纯 F12 后立即恢复原修饰键状态。
- F12 请求严格逐个等待返回，不再使用会丢弃请求类型的 30ms 超时，避免迟到结果被错误套用到下一阶段。

## 安装

将 `ZPlugs64` 中的内容复制到当前 ZBrush 用户目录的 `ZStartup\\ZPlugs64`，然后完整重启 ZBrush。

## 测试重点

- 普通右键旋转、Ctrl + Alt + 右键缩放/移动不受插件影响。
- 空白处按住左键时，最初的按下不进入 ZBrush，移动保持原生传递。
- 空白处按住左键拖入模型后能正常起笔。
- 模型直接起笔、UI、LightBox 点击和拖动正常。
